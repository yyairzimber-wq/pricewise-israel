# Vercel build (npm run publish:vercel): the app only; prices come from the daily
# snapshot that GitHub Actions publishes to GitHub Pages (.github/workflows/refresh-prices.yml).
VITE_DATA_MODE=static
VITE_STATIC_BASE_URL=https://yyairzimber-wq.github.io/pricewise-israel/data
VITE_AI_ENDPOINT=
