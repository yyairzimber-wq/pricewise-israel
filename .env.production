# Production web build: the app is served by the price server itself,
# so the API and AI live on the same origin under /pw-api.
VITE_DATA_MODE=api
VITE_API_BASE_URL=/pw-api
VITE_AI_ENDPOINT=/pw-api/recognize
